services:
  qdrant:
    image: qdrant/qdrant:v1.13.4
    container_name: chunking-docs-qdrant
    restart: unless-stopped
    ports:
      - "${QDRANT_HTTP_PORT:-6333}:6333"
      - "${QDRANT_GRPC_PORT:-6334}:6334"
    volumes:
      - "${QDRANT_STORAGE_PATH:-./.local/qdrant}:/qdrant/storage"
